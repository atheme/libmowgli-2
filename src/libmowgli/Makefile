include ../../extra.mk

SHARED_LIB = ${LIBMOWGLI_SHARED_LIB}
STATIC_LIB = ${LIBMOWGLI_STATIC_LIB}

SUBDIRS = ${LIBMOWGLI_MODULES} platform

INCLUDES = mowgli.h

include ../../buildsys.mk

LIBS += ${PTHREAD_LIBS}
CPPFLAGS += -I. -I../.. -DMOWGLI_CORE

LIB_OBJS += ${LIBMOWGLI_SHARED_MODULES}
OBJS += ${LIBMOWGLI_STATIC_MODULES}

